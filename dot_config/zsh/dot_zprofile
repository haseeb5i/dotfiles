#
# Executes commands at login; pre-zshrc
#

# Browser
if [[ -z "$BROWSER" && "$OSTYPE" == darwin* ]]; then
  export BROWSER='open'
fi

# Editors
export EDITOR='nvim'
export VISUAL='nvim'
export PAGER='less'

# Languages
if [[ -z "$LANG" ]]; then
  export LANG='en_GB.UTF-8'
fi

# Paths
# Ensure path arrays do not contain duplicates.
typeset -gU cdpath fpath mailpath path

# Set the list of directories that cd searches.
# cdpath=(
#   $cdpath
# )

# Set the list of directories that Zsh searches for programs.
path=(
  /usr/local/{,s}bin(N)
  $HOME/.local/{,s}bin(N)
  $path
)

# Less
# Set the default Less options, -X disables mouse scrolling
if [[ -z "$LESS" ]]; then
  export LESS='-g -i -M -R -S -w -X -z-4'
fi

# Development
export LOCAL_BIN_DIR=$HOME/.local/bin # quote or no-quote?
# Node
export NVM_DIR=$XDG_DATA_HOME/nvm
# export PNPM_HOME=$XDG_DATA_HOME/pnpm # can be changed?
# export PATH=$PATH:$PNPM_HOME
export PNPM_HOME=$LOCAL_BIN_DIR
export NPM_CONFIG_USERCONFIG=$XDG_CONFIG_HOME/npm/npmrc
export NODE_REPL_HISTORY=$XDG_DATA_HOME/node_repl_history
alias yarn='yarn --use-yarnrc "$XDG_CONFIG_HOME/yarn/config"'

# Python
export PYENV_ROOT=$XDG_DATA_HOME/pyenv
command -v pyenv >/dev/null || export PATH=$PYENV_ROOT/bin:$PATH

# Scala
export COURSIER_BIN_DIR=$LOCAL_BIN_DIR # watch

# Rust
export CARGO_HOME=$XDG_DATA_HOME/cargo
export RUSTUP_HOME=$XDG_DATA_HOME/rustup
[ -f "$CARGO_HOME/env" ] && \. "$CARGO_HOME/env"

# Golang
export GOPATH=$XDG_DATA_HOME/go
export GOMODCACHE=$XDG_CACHE_HOME/go/mod
export PATH=$PATH:/usr/local/go/bin:$GOPATH/bin
