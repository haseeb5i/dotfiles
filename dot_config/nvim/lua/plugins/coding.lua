return {
  {
    "saghen/blink.cmp",
    ---@module 'blink.cmp'
    ---@type blink.cmp.Config
    opts = {
      -- completion = { 
      --   menu = { border = "single" },
      --   documentation = { window = { border = "single" } },
      -- },
      -- signature = { window = { border = "single" } },
    },
  },
}
